import 'package:flutter/material.dart';

class S4534 extends StatelessWidget {
  const S4534({super.key});
  @override
  Widget build(BuildContext context) {
    return AppAkadamieAbstand64();
  }
}

class AppAkadamieAbstand64 extends StatelessWidget {
  const AppAkadamieAbstand64({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      spacing: 64,
      children: [Text("App"), Text("Akademie")],
    );
  }
}
